import navFooter from '@/data/navFooter.js'

const state = {
  navFooter: navFooter
}

const mutations = {}

const actions = {}

const getters = {
  navFooter: state => {
    return state.navFooter
  }
}

export default {
  state,
  mutations,
  actions,
  getters
}
